## 任务
生成 `src/components/ThemeToggle.tsx`

## 要求
- 使用 useThemeStore 获取主题状态
- 使用 shadcn/ui 的 Button 组件
- 使用 lucide-react 的 Sun 和 Moon 图标
- 点击按钮切换主题
- 添加 useEffect 监听主题变化，在 document.documentElement 上添加/移除 'dark' class

## 输出
只输出完整的代码文件内容，不要额外解释。