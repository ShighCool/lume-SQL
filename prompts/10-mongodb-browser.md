## 任务
生成 `src/components/MongoDBBrowser.tsx`

## 要求
- 左右布局：左侧显示集合列表，右侧显示文档
- 点击集合显示该集合的文档（使用 DataTable 组件）
- 支持添加新文档、编辑文档、删除文档
- 支持简单的查询过滤（输入 JSON 格式的 filter）
- 操作暂时用 console.log 模拟

## 输出
只输出ok,不要额外解释。