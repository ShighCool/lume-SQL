## 任务
生成 `src/stores/themeStore.ts`

## 要求
- 使用 zustand 创建 store
- 使用 persist 中间件持久化（key 为 'app-theme'）
- 包含状态：theme（'light' 或 'dark'）
- 包含方法：toggleTheme、setTheme

## 输出
只输出完整的代码文件内容，不要额外解释。