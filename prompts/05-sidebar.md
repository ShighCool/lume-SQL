## 任务
生成 `src/components/Sidebar.tsx`

## 要求
- 使用 useConnectionStore 获取连接列表和操作方法
- 使用 shadcn/ui 的 Button、ScrollArea 组件
- 使用 lucide-react 的 Database、Plus、Trash2、Plug、PlugOff 图标
- 显示所有连接，按类型分组（MySQL/Redis/MongoDB）
- 每个连接显示名称和状态（已连接/未连接）
- 点击连接切换 activeConnectionId
- 点击添加按钮打开 ConnectionForm 对话框
- 点击删除按钮删除连接
- 使用 Tailwind CSS 布局：固定宽度 260px，深色/浅色适配

## 输出
只输出完整的代码文件内容，不要额外解释。